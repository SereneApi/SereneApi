<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
	  <TargetFramework>netstandard2.1</TargetFramework>
    <AssemblyName>DeltaWare.SereneApi</AssemblyName>
    <RootNamespace>SereneApi</RootNamespace>
    <RepositoryUrl>https://github.com/armorall171/SereneApi</RepositoryUrl>
    <RepositoryType>GIT</RepositoryType>
    <PackageTags>API; REST; RESTful</PackageTags>
    <Description>Serene API is a small library intended to provide a straightforward way of consuming RESTful APIs requiring as little code &amp; setup as possible whilst providing a powerful set of tools.</Description>
    <Authors>Bradley Clarke</Authors>
    <Company>DeltaWare</Company>
    <Product>SereneApi</Product>
    <PackageProjectUrl>https://github.com/armorall171/SereneApi#what-is-serene-api</PackageProjectUrl>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Version>1.0.3.2-alpha</Version>
    <NeutralLanguage>en-AU</NeutralLanguage>
    <PackageLicenseFile>LICENSE.md</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <Copyright>Copyright (c) 2020 Bradley Clarke</Copyright>
    <PackageIcon>nuget_logo.png</PackageIcon>
    <PackageId>SereneApi</PackageId>
    <PackageReleaseNotes>* Added: CreateApiHandler extension to HttpClient, allowing for easy creation of Handlers. At present once done, the HttpClient will be disposed of by the Handler. In future there may be a method added in the builder to override this.
* Added: Multiple Readonly Properties to the ApiHandler allowing more information to be presented at runtime, this includes Source, Resource, Timeout and RetryCount.
* Removed: AddDependencies to the ApiHandler, this means you will only have access to the Dependencies in the constructor and should be making dependencies readonly.
* Removed: Some visible method extensions for HttpClient. These are not required for the End Users use.
* Moved: All Factories into the Factory namespace.</PackageReleaseNotes>
  </PropertyGroup>

  <ItemGroup>
	  <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="3.1.4" />
	  <PackageReference Include="System.Text.Json" Version="4.7.2" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\nuget_logo.png">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
    <None Include="..\..\LICENSE.md">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
  </ItemGroup>

</Project>
